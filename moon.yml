# https://moonrepo.dev/docs/config/project
$schema: 'https://moonrepo.dev/schemas/project.json'

# The workspace root is treated as a tooling project that owns shared tasks.
layer: 'tool'

project:
  name: 'cuan-root'
  description: 'Shared monorepo tooling and root-level tasks'

# Shared tasks that operate across the entire repository.
tasks:
  format:
    command: 'prettier --write .'
    toolchains: ['javascript', 'node']
    options:
      runInCI: false

  format-check:
    command: 'prettier --check .'
    toolchains: ['javascript', 'node']

  lint:
    command: 'eslint .'
    toolchains: ['javascript', 'node']

  install:
    command: 'bun install'
    toolchains: ['javascript', 'bun']
    options:
      runInCI: false
